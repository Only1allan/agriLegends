[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    ingestion: Tests for data ingestion layer
    processing: Tests for graph processing layer
    reporting: Tests for reporting layer
    integration: End-to-end integration tests
